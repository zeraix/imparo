[package]
name = "imparo-host"
description = "Backend-agnostic host infrastructure: fingerprint, tuned-config store, search-space versioning"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
imparo-backend = { path = "../imparo-backend" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.11"

[lints]
workspace = true
